{
  "permissions": {
    "allow": [
      "PowerShell(try { $r = Invoke-WebRequest -Uri \"http://localhost:19070/deployments\" -UseBasicParsing -TimeoutSec 3; $r.Content } catch { \"Admin API error: $\\($_.Exception.Message\\)\" })"
    ]
  }
}
